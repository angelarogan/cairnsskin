# Cairns Skin does not currently require environment variables to build
# or run locally. This file is a placeholder for future automation
# (e.g. a content-draft pipeline) so contributors know where secrets
# would be declared without ever committing real values.

# Example (not yet used):
# ANTHROPIC_API_KEY=
